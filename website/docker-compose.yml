services:
  statusline-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: antigravity-cli-statusline-site
    restart: unless-stopped
    ports:
      - "3030:80"
    environment:
      - TZ=Europe/Kyiv
